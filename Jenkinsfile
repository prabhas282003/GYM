pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo '✅ Cloned from GitHub'
            }
        }

        stage('Build') {
            steps {
                echo '🛠️ Building project...'
            }
        }
    }
}
